import React from 'react';

const TopBar: React.FC = () => (
  <div className="bg-gradient-to-r from-purple-600 to-blue-500 text-white text-sm text-center py-2 px-4">
    <p>
      Compare rates from multiple lenders with a single application —{' '}
      <span className="font-semibold underline">without affecting your credit score.</span>
    </p>
  </div>
);

export default TopBar;
